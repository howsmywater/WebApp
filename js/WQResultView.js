import React, { Component } from 'react';
import styled from 'styled-components';

export default class WQResultView extends Component {
    render() {
        const ViewWrapper = styled.div`
            display: flex;
        `;

        return (
            <div>
                hello
            </div>
        );
    }
}
